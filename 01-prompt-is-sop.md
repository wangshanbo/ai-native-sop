# 你的 prompt 不是 prompt，是 SOP

> 月烧 $2,000 AI 的反常识 SOP · 连载 1 / 10
>
> 作者：王善波 · 台满满科技技术合伙人 · 月烧 $2,000 / 6 亿 tokens 在 Claude Opus 等顶级模型上

---

我现在每个月在 Claude Opus 等顶级大模型上的支出大约是 **$2,000**，token 用量持续在 **6 亿+**。

很多人问我两个问题：
- "你怎么用得这么多？"
- "这么多到底有没有 ROI？"

回答这两个问题之前，我先反问你：**你打开 Cursor / Claude / ChatGPT 之后，第一句话通常是什么？**

如果你的答案是 "帮我写个 X" / "帮我修这个 bug" / "解释一下这段代码"——那么你已经站在 AI 时代生产力金字塔的**最低**那一格上。

不是 AI 不够好。是**你把 AI 当工具，而不是当员工**。

---

## 一个上周刚发生的真实场景

上周我让团队的一个工程师用 AI 修一个 bug。他打开 Cursor，敲下：

> "帮我看看 `OrderServiceImpl` 里的 `startTable` 方法，开台失败时金额计算不对"

AI 看了那段代码，给出了一个看起来非常合理的修复，工程师 commit、push、merge。

3 天后 QA 报告：开台金额对了。但 **VIP 卡余额扣减出错了**。

为什么？

因为 AI 看到的 context 只有那一个方法。**它根本不知道**：

- 这家公司"开台"涉及 7 个 Service 协作
- 金额扣减有一套幂等约定
- 修这个 bug 必须考虑事务边界（事务内不能直接发 MQ）
- 公司有专门的 ArchUnit 规则禁止某些写法
- BigDecimal 必须用 `setScale(2, ROUND_HALF_UP)`，不能用 `setScale(2)`

工程师的"prompt"是合格的。**但他给 AI 的"上岗培训"是 0**。

如果他把 AI 当成新员工来对待，他会让对方第一天就上手改 `OrderServiceImpl` 吗？不会。
但他对 AI 这么做了。

---

## 把每个 prompt 当成给新员工的 SOP

我现在打开 Cursor 的第一件事，**不是问问题**，是确认 AI 的"上岗状态"。

具体地说，我每个生产项目根目录里永远存在这样的文件结构：

```
project-root/
├── CLAUDE.md                    # 项目入口（给所有 AI agent 看）
├── AGENTS.md                    # Cursor / Claude Code 等通用 agent 入口
├── .cursor/
│   └── rules/                   # 项目级硬规则（自动加载）
│       ├── 00-dispatcher.mdc    # 分诊规则：什么需求走什么流程
│       ├── coding-style.mdc     # 代码风格硬约束
│       └── strict-review.mdc    # 提交前自查清单
├── .claude/
│   └── skills/                  # 可被加载的"技能"（按需加载）
│       ├── pos-server-business/ # 业务规则（订单、支付、预付金语义）
│       ├── pos-server-impl/     # 实现守门（事务、锁、SQL、BigDecimal）
│       └── pos-server-review/   # 提交前严格审查清单
└── doc/
    └── *.md                     # 业务背景与历史决策
```

每次对话，AI 在响应之前**必然加载**这些规则。这不是"我让它读文档"——是**它必须读，否则它没资格写代码**。

这就是 SOP——Standard Operating Procedure。

一个新员工入职第一周做的事：读公司 wiki、看代码规范、跟前辈聊业务、看 PR review 历史。
**我现在让 AI 每次对话之前重新做这一切**。

听起来很重？很重。
但**这就是为什么我月烧 $2,000 还觉得划算**。

---

## 为什么这一点是反常识

行业现在的主流叙事是：

- "Prompt engineering 是过时技能"
- "新模型自己就懂"
- "你只需要描述意图，模型自己 figure it out"

我的实战反馈：**全错**。

新模型理解力是变强了，**但它依然不懂你公司的业务**。

它依然不懂"我们 OrderServiceImpl 里面为什么不能直接 `mqService.publish(...)`"，
它依然不懂"我们 BigDecimal 精度规则到底是 2 位还是 4 位"，
它依然不懂"我们 review 时哪些写法直接 fail，哪些需要豁免理由"。

这些**永远不会进入大模型的训练集**。
这些只能通过你自己组织的"上下文"喂给它。

**而把这些上下文写好、组织好、版本化、按需加载**——

**这就是 AI 时代真正的硬技能。**

它的名字不是 prompt engineering。
它的名字是 **context engineering**。

---

## 实战：4 层上下文金字塔

我的项目里，AI 拿到的 context 大概是这样组织的（从全局到局部）：

```
┌─────────────────────────────────────────────┐
│  第 1 层：公司级 SOP                           │
│  CLAUDE.md / AGENTS.md                      │
│  → 公司纪律、写给所有 AI 的总说明                │
└─────────────────────────────────────────────┘
              ↓ 默认加载
┌─────────────────────────────────────────────┐
│  第 2 层：项目级硬规则                          │
│  .cursor/rules/*.mdc                        │
│  → 不可妥协的约束（事务、锁、命名、依赖）          │
└─────────────────────────────────────────────┘
              ↓ 默认加载
┌─────────────────────────────────────────────┐
│  第 3 层：可加载技能                            │
│  .claude/skills/*/SKILL.md                  │
│  → 按需加载（业务领域、实现细节、审查清单）        │
└─────────────────────────────────────────────┘
              ↓ 模型自己判断该加载哪些
┌─────────────────────────────────────────────┐
│  第 4 层：当前任务上下文                        │
│  你的 prompt + 相关文件                       │
│  → 大多数人只在这一层和 AI 工作                  │
└─────────────────────────────────────────────┘
```

**普通用户只在第 4 层和 AI 工作**。
**我让 AI 默认带上前 3 层，然后才看第 4 层**。

这就是为什么同样的模型、同样的 token、同样的需求，
**我的 AI 输出质量是普通用户的 5~10 倍**。

不是模型不一样，是**上下文不一样**。

---

## 为什么这值 $2,000 / 月

很多人算账："$2,000 一个月够请半个工程师了。"

我的账是这样算的：

| 项 | 数据 |
|---|---|
| AI 帮我交付的工作量（按"如果让一个 senior 写需要多久"估）| 约 4~5 个 senior 工程师月产出 |
| Senior 工程师月薪（含五险一金）| 约 $4,500 |
| 等价人工成本 | $18,000~$22,000 / 月 |
| 我的 AI 投入 | $2,000 / 月 |
| **杠杆倍数** | **9~11 倍** |

但这只在**你做了 SOP**的前提下成立。

如果你只在第 4 层和 AI 工作（"帮我写个 X"），你的杠杆大概是 1.2~1.5 倍——
**那就真的不如请人**。

---

## 怎么开始：3 个今天就能做的动作

如果你想把"普通 AI 用户"变成"AI native 工程师"，今天就做这 3 件事：

**1. 给你每个项目根目录加一个 `CLAUDE.md` / `AGENTS.md`**

用 5 行写清楚你这个项目的核心约束：

```markdown
# AGENTS

## 项目核心约束（不可违反）

- 数据库：所有金额字段用 BigDecimal，必须 setScale(2, ROUND_HALF_UP)
- 事务：@Transactional 内禁止直接发 MQ / 调外部 HTTP，必须 afterCommit
- 命名：Service 实现类强制以 ServiceImpl 结尾
- 依赖：未经审批不得新增 pom.xml 依赖
- 删除：未经明确授权不得删除任何文件 / 接口能力

## 代码风格

- 中文注释 OK，但接口名 / 类名 / 变量名必须英文
- 单方法 ≤ 80 行；超过必须拆分
- 拒绝沿用历史 God Class 作模板（OrderServiceImpl 4700 行不是榜样）
```

5 分钟写完。**今天**就能写。

**2. 建一个 `.cursor/rules/` 文件夹**

把团队 review 时反复说的话、踩过的坑、必须遵守的规则，写成 `.mdc` 文件。

`.cursor/rules/strict-review.mdc` 示例片段：

```markdown
---
description: "提交前自查清单"
globs: "**/*.java"
alwaysApply: true
---

# 提交前自查（用户说"审查"或"自查"必须走完）

1. 是否新增了未审批的 pom 依赖？
2. 是否在事务内直接发 MQ / 调 HTTP？
3. 是否使用了 BigDecimal 但没设 scale？
4. 是否新增了 100 行以上的 Service 方法？
5. 是否破坏了既有接口能力？
6. 是否漏处理 Exception 错误码？
```

让 AI 默认加载，每次写代码自带这把尺。

**3. 每次开新对话先确认它真的读到了**

让 AI 复述一下："你刚才看到的硬规则有几条？分别是什么？"

如果它复述不出来，**那就是没读到**——这时候继续问技术问题，等于让一个没培训过的新人上岗。

---

## 这只是入门

做满 30 天，你的 AI 工作流就会跟以前**完全不同**。

但这只是 4 层上下文金字塔里的**第 1 层**。

下一篇我会写：

> **月烧 $2,000 的工具组合论 —— 为什么我同时用 Cursor / Claude Code / 自研 NormCode / 不同模型分层路由**

如果你想第一时间看到，关注我的 [即刻 @王善波]、[GitHub @wangshanbo](https://github.com/wangshanbo)，或留言 +1。

---

## 关于作者

我是王善波，36 岁，杭州。**台满满科技技术合伙人**，11 年 B2B SaaS 实战。

每月在 Claude Opus 等顶级 AI 模型上花 **$2,000 / 6 亿 tokens**，把 AI 用成生产力，不是玩具。

自研 [NormCode](https://github.com/wangshanbo/NormCode) —— fork VS Code 的 AI IDE，内置 **Sentinel 长程 Harness**，对标 Anthropic 工作范式。

这个连载会用 6 周时间，把我月烧 $2,000 学到的反常识 SOP 全部公开。共 10 篇。

**Disclaimer**：本系列所有方法都是我个人在生产项目上的实战总结，不是 best practice 圣经。看完觉得有用就用，觉得不对就拍砖。评论区见。
